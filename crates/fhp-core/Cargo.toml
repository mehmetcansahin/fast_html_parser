[package]
name = "fhp-core"
version = "0.1.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Core types for the HTML parser: interned tags, entity table, error types"
homepage.workspace = true
repository = "https://github.com/mehmetcansahin/fast_html_parser"
documentation = "https://docs.rs/fhp-core"
keywords = ["html", "parser", "core", "tags", "entities"]
categories = ["parser-implementations"]
exclude = ["benches/", "tests/"]

[dependencies]
thiserror = { workspace = true }
phf = { workspace = true }
